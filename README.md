## vue-lib

Vue component library

### setup

```bash
$ brew install direnv
$ cp .envrc.dev .envrc
$ direnv allow
```

## dir

- dev: 開発時の動作確認用ディレクトリ
- packages: npm package の配信対象

## publish

```bash
npm version patch
npm version minor
npm version major
```
